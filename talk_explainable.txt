Make this scripts better divided in proper functions with each one of them with well made documentation.
At each step of each function print to the user what is happening.
At each step treat every possible error and print proper error messages.
At each success print to the user what is happening.
At each cicle of a functionallity print the progress percentage.
At each cicle of a functionallity treat every possible error and print proper error messages.
At each cicle of a functionallity print to the user what is happening.
At each cicle of a functionallity treat every possible error and print proper error messages.
At each cicle of a functionallity print the summary of the cicle.
At each cicle of a functionallity print the summary with a total of success and total of fails.
Give a visual representation very explicit and clear for the summaries.
